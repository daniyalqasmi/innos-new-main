import exp from "constants";

export type Feature = {
  id: number;
  icon: JSX.Element;
  title: string;
  paragraph: string;
  // btn: string;
  btnLink: string;
};
export type Button={
  btn: string;
}
